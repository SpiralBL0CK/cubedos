-- This file is used to ensure that the code is Jorvik and SPARKable by default.
--
-- Note that SPARK_Mode is set in the individual files. Not all sources are in SPARK.
--pragma SPARK_Mode(On);

pragma Profile(Jorvik);
pragma Partition_Elaboration_Policy(Sequential);
--pragma Warnings(Off, "may call Last_Chance_Handler");
